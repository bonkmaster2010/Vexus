{
  "version": 2,
  "buildCommand": "CI=false SKIP_PREFLIGHT_CHECK=true npm run build",
  "framework": "create-react-app",
  "builds": [
    { "src": "package.json", "use": "@vercel/static-build" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/" }
  ]
}
